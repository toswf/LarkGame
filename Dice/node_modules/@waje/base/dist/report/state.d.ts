export interface ReportState {
    queue: Array<() => Promise<void>>;
    initialized: boolean;
    pageTimeMap: Map<string, number>;
}
export declare function createReportState(): ReportState;
/**
 * 处理队列中的任务
 */
export declare function processQueue(state: ReportState): Promise<void>;
export declare const reportState: ReportState;
