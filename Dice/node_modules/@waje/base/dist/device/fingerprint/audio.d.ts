/**
 * 音频指纹模块
 * 利用 AudioContext 的采样率和声道数等稳定属性生成指纹。
 */
/**
 * 获取音频指纹
 *
 * 仅采集 AudioContext 的稳定属性（采样率、声道数），
 * 避免使用动态振荡器数据，确保跨会话一致性。
 *
 * @returns 音频指纹字符串；不支持时返回 "no-audio-context" / "audio-error"
 */
export declare const getAudioFingerprint: () => string;
